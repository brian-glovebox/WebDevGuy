import React from 'react';
import useStyles from './useStyles'

const Main = (): JSX.Element => {
    const classes = useStyles();

    return (
        <>

        </>
    );
};

export default Main;
